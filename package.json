{
  "author": "Jesper Nellemann Jakobsen",
  "private": true,
  "name": "betrayal-pwa",
  "version": "0.0.1",
  "description": "A Progressive Web App for keeping score of character stats in Betrayal at House on the Hill.",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "devDependencies": {
    "classnames": "^2.2.5",
    "eslint": "^4.5.0",
    "if-env": "^1.0.0",
    "node-sass": "^4.7.2",
    "normalize.css": "^7.0.0",
    "preact-cli": "^3.4.3",
    "preact-helmet": "^4.0.0-alpha-3",
    "sass-loader": "^13.2.0"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0"
  }
}
